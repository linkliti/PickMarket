""" Server Module """
import logging

log = logging.getLogger(__name__)


def server() -> None:
  """ Server """
  # c: dict[str, lisst] = ozonParserCategories().getRootCategories()
  # with open(file='categories.json', mode='w', encoding="utf-8") as f:
  #   json.dump(c, f, ensure_ascii=False)
